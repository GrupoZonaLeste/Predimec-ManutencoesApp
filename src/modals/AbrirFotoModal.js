import {View, Text, StyleSheet, Modal} from 'react-native'

const AbrirFotoModal = ({modalVisible, setModalVisible, fotoBase64}) => {
  return(
    <></>
  )
}

const styles = StyleSheet.create({
    
})

export default AbrirFotoModal